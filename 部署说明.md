# iTOF 在线解压显示程序部署说明

## 1. 运行环境

本程序面向原生 Ubuntu/Linux 系统，不依赖 WSL，也不要求使用固定用户名或固定安装目录。

建议环境：

- Ubuntu 18.04 或更新版本
- Python 3.6 或更新版本，仅使用 Python 标准库
- CERN ROOT，需包含 HTTP 和 OpenGL 支持
- 与 ROOT 版本兼容的 GCC/G++ 编译器

当前已验证的开发环境为：

- Python 3.6.9
- ROOT 6.26/04
- GCC 7.5

程序默认使用以下端口：

- `8088`：网页控制服务
- `8090`：ROOT HTTP/JSROOT 服务

部署前请确认这两个端口未被其他程序占用。

## 2. 复制项目

将整个 `runtimedisplay` 文件夹复制到目标 Ubuntu 电脑上的任意目录，例如：

```bash
/home/user/software/runtimedisplay
```

项目路径和用户名可以自由修改。启动脚本会根据自身位置自动确定项目根目录，无需修改源代码。

确保当前用户拥有项目目录的读写权限：

```bash
cd /home/user/software/runtimedisplay
chmod +x itof/online/*.sh itof/online/control_server.py
```

如果项目由其他用户或 `root` 复制，可先修改所有者：

```bash
sudo chown -R "$(id -un):$(id -gn)" /home/user/software/runtimedisplay
```

## 3. 配置 ROOT

先检查 ROOT 是否已经可用：

```bash
root-config --version
root -b -q
```

如果 `root` 已经在 `PATH` 中，可以直接启动程序。

如果尚未加载 ROOT 环境，设置 `ROOT_SETUP`：

```bash
export ROOT_SETUP=/path/to/root/bin/thisroot.sh
```

也可以先手动加载 ROOT：

```bash
source /path/to/root/bin/thisroot.sh
```

程序会依次从当前 `PATH`、`ROOT_SETUP`、`ROOTSYS` 以及常见 ROOT 安装位置寻找 ROOT，不依赖原电脑上的 ROOT 路径。

## 4. 启动程序

进入项目目录：

```bash
cd /home/user/software/runtimedisplay
```

启动或重启控制服务：

```bash
./itof/online/restart_control_demo.sh 8088
```

本机访问：

```text
http://127.0.0.1:8088/
```

局域网内其他电脑访问时，将 `127.0.0.1` 替换为服务器 IP：

```text
http://服务器IP:8088/
```

如启用了 Ubuntu 防火墙，需要开放控制端口和 ROOT 端口：

```bash
sudo ufw allow 8088/tcp
sudo ufw allow 8090/tcp
```

## 5. 项目内资源

程序运行所需资源均保存在项目内：

- `itof/data/`：监控数据目录
- `itof/reco/map/`：电子学 map 文件
- `itof/macro/newchip/Calib_iTOF.root`：文件标定模式使用的标定文件
- `itof/macro/newchip/CLSB.txt`：细时间标定数据
- `itof/geo/itof_geomanager_vbatch2409.root`：探测器几何文件
- `itof/online/`：在线监控、网页控制服务和启动脚本
- `itof/online/logs/`：运行日志和网页设置

默认的 `self` 标定模式不读取 `Calib_iTOF.root`。需要使用文件标定时，在网页中选择对应模式和项目内的标定文件。

## 6. 数据与网页设置

网页中的 `Data path` 应填写数据总目录，例如：

```text
/home/user/software/runtimedisplay/itof/data
```

程序会自动扫描该目录下一级、名称符合 `yyyymmdd_n` 格式的子文件夹。

网页设置保存在：

```text
itof/online/logs/web_settings.json
```

项目迁移后，如果其中仍保存原电脑的项目内绝对路径，服务会根据 `/itof/...` 后缀自动重定位到新项目目录。项目外部的数据路径不会被擅自修改。

## 7. 停止与查看日志

可以在网页上使用 Stop 按钮停止 ROOT 在线监控。

控制服务日志位于：

```text
itof/online/logs/runtimedisplay_control_当前用户名.log
```

ROOT 监控日志位于：

```text
itof/online/logs/runtimedisplay_root_monitor.log
```

实时查看日志：

```bash
tail -f itof/online/logs/runtimedisplay_control_"$(id -un)".log
tail -f itof/online/logs/runtimedisplay_root_monitor.log
```

## 8. 常见问题

### ROOT executable not found

说明 ROOT 尚未加载。执行：

```bash
export ROOT_SETUP=/path/to/root/bin/thisroot.sh
./itof/online/restart_control_demo.sh 8088
```

### Permission denied

检查项目及日志目录是否属于当前用户：

```bash
ls -ld . itof/online/logs
sudo chown -R "$(id -un):$(id -gn)" .
```

不要使用 `sudo` 启动程序，否则可能产生普通用户无法覆盖的日志、PID 文件和 ROOT 编译缓存。

### 网页可以打开，但图像无法加载

依次检查：

```bash
ss -ltn | grep -E ':8088|:8090'
tail -n 100 itof/online/logs/runtimedisplay_root_monitor.log
```

同时确认数据目录、电子学 map 和 ROOT 环境配置正确。

### 端口已占用

检查占用进程：

```bash
ss -ltnp | grep -E ':8088|:8090'
```

控制页端口可以通过启动脚本参数修改，例如：

```bash
./itof/online/restart_control_demo.sh 8089
```

ROOT 服务默认仍使用 `8090`，需要保证该端口可用。
